[pytest]
# Coloca a raiz do repositório no sys.path para que `import app` funcione
# independentemente de como o pytest é invocado (`pytest` ou `python -m pytest`),
# tanto localmente quanto no CI.
pythonpath = .
testpaths = tests
